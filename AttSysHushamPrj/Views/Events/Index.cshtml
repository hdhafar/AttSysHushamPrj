@model IEnumerable<AttSysHushamPrj.Models.Event>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>


@* @using (Html.BeginForm("CheckIn","Events",FormMethod.Post))
{
    <button id="update-profile-button" type="btn btn-primary" class="btn btn-primary">Check In</button>
    @* <input class="btn btn-primary" tpye="submit" value="Check In"/> *@

@*
@using (Html.BeginForm("CheckOut", "Events", FormMethod.Post))
{
    <button id="update-profile-button" type="btn btn-primary" class="btn btn-danger">Check Out</button>
    @* <input class="btn btn-primary" tpye="submit" value="Check In"/> 
}*@


@* <p>
    <a asp-action="Create">New Employee</a>
</p> *@
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.EventDate)
            </th>
        @*     <th>
                @Html.DisplayNameFor(model => model.EventTime)
            </th> *@
            <th>
                
                @Html.DisplayNameFor(model => model.EventType)
            </th>
            <th>

                <p>violation</p>
            </th>
            <th>

                <p>Justification</p>
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.EventDate)
            </td>
       
            <td>
                    @if (item.EventType == 1)
                    {
                        <p>IN</p>
                    }
                    else if (item.EventType == 2)
                    {
                        <p>OUT</p>
                    }
                   
            </td>
            <td>
                @if(item.IsLate == true || item.IsEarly == true)
                {
                        <p>Attendance not regular</p>
                    }
                    else
                {}
            </td>
            <td>
                    @Html.DisplayFor(modelItem => item.Justification)
            </td>
            <td>
                    <a asp-action="Edit" asp-route-id="@item.EventID">Add Justification</a> |
              @*   <a asp-action="Details" asp-route-id="@item.EventID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.EventID">Delete</a> *@
            </td>
        </tr>
}
    </tbody>
</table>
